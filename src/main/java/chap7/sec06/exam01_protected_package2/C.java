package chap7.sec06.exam01_protected_package2;

import chap7.sec06.exam01_protected_package1.A;

public class C {


    /*public void method() {
        A a = new A();  //같은 패키지라서 생성 가능
        a.field = "value";  //같은 패키지라서 필드 접근 가능
        a.method(); //같은 패키지라서 호출 가능
    }*/ //다른 패키지고 자식 클래스가 아니라서 접근 불가 (protected)
}
